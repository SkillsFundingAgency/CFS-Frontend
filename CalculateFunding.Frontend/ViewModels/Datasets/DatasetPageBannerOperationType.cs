namespace CalculateFunding.Frontend.ViewModels.Datasets
{
    //Enum used to identify if user is arriving at managedatset page via Create dataset OR Update Dataset pages
    public enum DatasetPageBannerOperationType
    {
        DatasetUpdated,
        DatasetCreated,
    }
}
